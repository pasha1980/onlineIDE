{% extends 'base.html.twig' %}

{% block title %}
    Project {# todo #}
{% endblock %}

{% block body %}

    {% if user == currentUser %}
    {{ form(newFolder) }}
    <hr>
    {{ form(newFile) }}
    {% endif %}

{% endblock %}